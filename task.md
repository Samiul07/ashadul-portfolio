- [x] Find the correct portrait node named "magnific_rlqg3OHxtc 3" (ID `3248:737`) in Figma
- [x] Export the high-resolution correct portrait image by its hash from Figma Desktop
- [x] Save the portrait image to `public/images/magnific-portrait.png`
- [x] Update `app/page.tsx` to use the correct image path
- [x] Apply Figma dimensions of 704x949 to the image and container
- [x] Fix squished aspect ratio of portrait image by applying `object-cover`
- [x] Align the image to `bottom-0` and the right side border line (`right: max(calc((100vw - 1400px) / 2), 24px)`)
- [x] Adjust Client Feedback card size: width 404px and height 210px
- [x] Align feedback widget to bottom-8 and right-8 relative to the content padding
- [x] Set outer padding to 20px top/bottom, 24px left/right
- [x] Update header label to `// Client Feedbacks` and tracking-normal
- [x] Set quote text size to 16px and line-height to 1.2
- [x] Configure uppercase styling for client name, role, and company
- [x] Adjust feedback text lengths so every quote wraps to exactly 3 lines
- [x] Extract `Switzer_Complete.zip` local font archive to `public/fonts/switzer`
- [x] Set up Next.js local font loader (`localFont`) mapping for Switzer Variable WOFF2 files
- [x] Configure Tailwind theme mapping `--font-sans` to resolve locally hosted Switzer font
- [x] Delete unused Google Sans directories and ZIP files
- [x] Restore animating progress bar at the bottom of the Client Feedback card
- [x] Export portrait node with Progressive Layer Blur effect applied (radius 60, offset 83.7% to 100%) from Figma
- [x] Overwrite `public/images/magnific-portrait.png` with the blurred portrait version
- [x] Reposition Main Gradient layer vertical offset to `top-[541px]` (matching the Figma coordinates exactly)
- [x] Ensure Main Gradient layer (`z-4`) overlays the portrait container (`z-3`) to blend the bottom of the photo into the red background
- [x] Remove `overflow-x-clip` from the Hero Section to allow the red background glows to bleed smoothly without a sharp horizontal cut-off line
- [x] Extract availability badge to `components/hero/availability-badge.tsx` client component
- [x] Configure floating vertical animation (`y: [0, -4, 0]`) on the envelope icon using Framer Motion
- [x] Keep shape-matched white pulse centered on the icon during floating
- [x] Shift portrait container vertically using `bottom-[48px]` to allow hair and head to cross into the header area
- [x] Restore static header z-index to `z-[200]` and remove dynamic scrolling z-index pop
- [x] Remove fixed bottom separator line from Navbar header
- [x] Remove duplicate static line from `app/page.tsx`
- [x] Restore fixed Scroll Progress Bar and bottom line in separate fixed container at `top-[85px] z-[99]`
- [x] Remove backdrop-filter from Navbar header transition classes to resolve browser repaint flickering/flashes
- [x] Configure background separator line size to `1px` and red scroll progress line to `2px`
- [x] Remove `transition-all duration-300` delay class from progress bar wrapper to eliminate the scroll-back head overlay lag
- [x] Increase StatsSection and SelectedWork z-indices to `z-[105]` to stack above the Main Gradient (`z-[102]`), keeping the text and numbers crisp and white
- [x] Relocate Main Gradient to `z-[2]` to place it in the absolute background behind the vertical/horizontal grid lines (`z-[98]` / `z-10`)
- [x] Apply GPU hardware acceleration layer promotion to the portrait container in `app/page.tsx`
- [x] Apply GPU hardware acceleration layer promotion to the navigation header wrapper in `components/layout/navbar.tsx`
- [x] Restore `StatsSection` and `SelectedWork` z-indices to `z-6`
- [x] Apply GPU hardware acceleration layer promotion to the progress bar container in `components/layout/navbar.tsx`
- [x] Reposition all `CrossMark` elements mathematically using `left-0 -translate-x-1/2` and `translate-y-1/2` relative to centered border wrappers to ensure pixel-perfect alignment at all screen resolutions
- [x] Unify all grid line colors, opacities, and widths to `white/12` across the entire page (Hero, Stats, Selected Work, Callout Notes, Field Notes)
- [x] Adjust the vertical line height of the `topHalf` `CrossMark` (T shape) from `h-6` (`24px`) to `h-3` (`12px`) to match the left and right arm lengths, creating a perfectly proportioned T shape
- [x] Save new `@2x` portrait image, scaling it to 50% (`697px x 939px` to include the optional 10px layout size reduction) while maintaining alignment
- [x] Resolve double border thickness at the top of the logo list section and ensure all horizontal dividers have matching thickness and full-screen-width span
- [x] Animate the custom vector glow shape on the hero background using hardware-accelerated Framer Motion translations
- [x] Verify static compilation builds successfully
