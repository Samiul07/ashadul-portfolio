# Taste
- Communicates in Bengali (Bangla) — writes bug reports and feedback in Bengali and expects the agent to reply in the same language rather than English. Confidence: 0.7
- Wants the agent to do a thorough top-to-bottom scan of the project and fully understand the codebase, current state, and recent history before starting any work — explicitly asks to be treated like a newly hired developer who must get up to speed first. Confidence: 0.9
- Builds projects with multiple AI assistants (Codex, Intergravity, Cursor) and expects the agent to pick up where those tools left off (checking task notes, logs, and other tool state) rather than treating the project as fresh. Confidence: 0.7
- Tasks are explicitly coordinated across the multiple AI assistants: when the user says a task is already handled by the others ("we have already done that with other three AI"), they expect the agent to drop it entirely and not re-execute or re-plan it; pending work is handed out explicitly ("I will let you know what the tasks we have in hand later on that is waiting for you"), so the agent should stand by and wait for the assignment rather than self-initiate previously discussed tasks. Confidence: 0.6
- Reuses existing project icons and patterns instead of inventing new ones — asked to swap a text glyph arrow for the project's standard arrow SVG, and explicitly stated the same applies to any other icons the agent creates. Confidence: 0.85
- Expects pixel-level visual polish: flags subtle inconsistencies (uneven border thickness, dim/bright variation, wrong spacing/cropping) and wants them corrected to be uniform and crisp. Confidence: 0.8
- When reporting visual problems, expects the agent to visually inspect the screenshot and fix all visible issues, not just the ones explicitly named. Confidence: 0.75
- Prefers removing UI elements that are redundant or don't make contextual sense (e.g., a button inside an author card that merely re-links to the same author section). Dislikes "over-engineered" designs: redundant nested boxes/frames ("box in a box"), decorative noise (glow shadows, chamfered corners, `//` code-prefix labels) on simple content like an author bio — wants clean, editorial simplicity instead. Confidence: 0.8
- Expects visual and consistency fixes to cover every viewport breakpoint — explicitly checks whether mobile-only UI (not just desktop/tablet) was addressed, e.g., mobile-only TOC toggle arrows and prev/next card arrows must get the same icon/visual treatment as desktop elements. Confidence: 0.85
- In visual layouts, considers an image/photo that doesn't match the height of the adjacent text column a real design flaw (e.g., a small square avatar next to a tall bio block) — expects media to be sized to balance the text block it sits beside. Confidence: 0.75
- Wants sizing/typography decisions validated against the site's own established house style before changing anything — when UI feels too small, audit the homepage, blog index, and portfolio pages for the site-wide scale (avatar sizes, button padding, text-size floors) and bring the outlier into line with it rather than guessing at sizes. Confidence: 0.85
- Dislikes unnecessary dead/empty vertical space after the footer or at the bottom of pages — wants the page to end cleanly with the bottom-most element (e.g., giant wordmark) flush to the page edge, and expects the fix to be done carefully ("wisely") so the intended design (anchored wordmark, blur fade) is preserved rather than clipped or hacked. Will follow up angrily if the dead space persists after a claimed fix. Confidence: 0.85
button padding, text-size floors) and bring the outlier into line with it rather than guessing at sizes. Confidence: 0.85
- Dislikes unnecessary dead/empty vertical space after the footer or at the bottom of pages — wants the page to end cleanly with the bottom-most element (e.g., giant wordmark) flush to the page edge, and expects the fix to be done carefully ("wisely") so the intended design (anchored wordmark, blur fade) is preserved rather than clipped or hacked. Will follow up angrily if the dead space persists after a claimed fix. Confidence: 0.6
- Iterative visual design sessions are the norm: expects a screenshot-based critique loop where each round of feedback is addressed and re-verified (screenshots/DOM geometry at desktop + mobile) before considering the work done; a design change usually takes multiple rounds before it's "100%". The user expects the implemented result to be shown visually and will call it out if the agent only claims completion without proof ("you didn't include the screenshot, did you?"). Follows up immediately with a fresh screenshot when the claimed fix still doesn't look right to them ("still not same"), so a fix isn't done until the user confirms it in their own view. Confidence: 0.95
- For redesign work, expects deep external research before implementing: explicitly asks to "launch multiple agents" to study Framer/Webflow templates and other trendy designs, then to "come back with answer, then apply it automatically" — research first, apply the findings yourself rather than returning with options for approval. Confidence: 0.9
- Before relying on framework/library behavior during a fix, reads the actual docs shipped with the installed version (e.g., `node_modules/next/dist/docs` for Next 16 `next/link` semantics) rather than assuming — the project's AGENTS.md flags breaking changes in that version, so behavior is confirmed against the real docs before code is changed. Confidence: 0.6
- When the user says "design system," they expect the agent to audit/confirm the rest of the website's established patterns before building anything new — "whenever I mention design system, I have to need to check out the rest of the website to confirm." Confidence: 0.8
- When flagging a specific issue (e.g., a spacing misalignment), the user explicitly expects a surgical fix — "fix it without harming anything else"; change only the offending element and leave everything else untouched, then verify the change compiles/builds cleanly. A fix that breaks other parts of the page (e.g., a spacer added for a fixed reading bar creating dead space on mobile) is immediately reported as a new bug and must be repaired in the same pass ("fix this at once"); the standing rule is to "make the smallest targeted change that solves the issue." Confidence: 0.9
- When a claimed fix still shows as broken to the user, systematically rule out stale renders before re-diagnosing: check both the dev server's SSR HTML and any static `out/` export (Next.js `output: "export"`) for leftover old classes/markup, since the user may be viewing a stale build — but also don't assume staleness; verify with a fresh capture of actual rendered pixels before concluding. A "(stale)" tag on the phone's dev overlay is a clue to check caching/logs, but the fresh code must still be audited for genuine mismatches (e.g., by diffing SSR HTML against the client-rendered DOM) rather than blaming the phone alone. Where the fix is confirmed but the phone still shows "(stale)", the dev server should be restarted so the next load serves the fresh bundle. Confidence: 0.95
- When the user reports a bug (e.g., a mobile bar shaking while scrolling, or a page that "reloads itself"), they explicitly ask the agent to "identify the cause from multiple angles" before fixing — expects a multi-candidate root-cause analysis (CSS/compositor effects, layout/positioning context, JS update patterns, transitions) rather than a single guess, then the fix. Confidence: 0.9
- When the user points at a suspected source for a bug (e.g., "the problem mainly started from the Contract page"), it is a directional lead, not the verdict — they expect the agent to verify it independently, audit beyond it (the same problem may exist elsewhere), and own the final diagnosis ("at the end of the day, it's you who needs to figure this out"). Confidence: 0.75
- Explicitly endorses launching/swapping multiple agents to dig into a project from different angles during bug-hunting ("if you need to swap multiple agents to go dig into the project from multiple steps, feel free to do it. In fact, that's one of my recommendations as well") — parallel independent audits that converge on a root cause are welcomed. Confidence: 0.7
- When the user frames a fix as a "challenge" and demands a "100% guarantee," they expect the agent to prove the solution by self-testing in the actual affected environment (e.g., headless-browser phone-sized emulation, measuring the element's position/height across scroll positions and confirming the progress UI still updates) before declaring done — code reasoning alone is not accepted as proof. Confidence: 0.8
- Reports bugs by sending a screenshot with a terse imperative ("fix this on phone") and expects the agent to visually diagnose the root cause from the image and fix it end-to-end without asking for clarification — dev-mode error overlays (e.g., Next.js hydration mismatch warnings) are treated as real bugs to eliminate (server/client first paint must match exactly, e.g., by gating client-only motion/animation elements behind a mounted flag), not dismissed as harmless dev artifacts. Confidence: 0.7
- For hydration mismatches caused by browser-extension-injected DOM attributes (e.g., `__gcruniqueid` on an `<input>`), expects the documented React fix — `suppressHydrationWarning` on the affected element — applied not just to the reported element but to every element of the same kind across the codebase, since the extension can strike any of them. Confidence: 0.85
- Tests the site on a physical phone over the LAN dev server (e.g., `192.168.0.242:3000`) in addition to desktop, so mobile-specific breakage that only appears in a real mobile browser environment must be reproduced, fixed, and verified in a mobile viewport with a clean console before declaring done. Confidence: 0.65
- Verification includes confirming the fix is actually live in the served output: after editing code, checks the HTML served by the running dev server (curl/grep for the new markup, e.g., zero remaining `href="#"` anchors, links rendering as spans) so "fixed" is proven against real rendered output, not just typecheck/build. Also respects an already-running dev server on the default port instead of fighting it for the port. Confidence: 0.7
- When asked to "start a localhost for the local network," expects the dev server to be bound to all interfaces (`npx next dev -H 0.0.0.0`) so the phone can reach it at the LAN IP, and expects the agent to verify it responds on both `localhost` and the LAN IP before confirming it's up. Confidence: 0.8
- Treats regression of previously-working behavior as a serious bug: when a later change breaks a feature that used to behave correctly (e.g., the reading bar now sitting over the hero on load instead of only appearing when reading starts), the user reacts angrily ("It was good until you made it bad... That's how it was, right?") and expects the original intended behavior restored and verified across its full lifecycle, not just patched. Confidence: 0.85
- When the user provides real profile/external links (social media URLs), they expect them wired into every obvious place on the site (shared footer icons, the contact page's social rail over the photo), not just the spot they named — "find every single place this needs to be linked, obvious factors, not unnecessary things." The search is comprehensive but sensibly scoped: dead/archived unused files are not treated as link targets and are left alone. Confidence: 0.75
- When the user reports something "not clickable/not linked" that the local code already implements correctly, verify against both local source and the live deployed site before editing: the production build may simply be stale (placeholder URLs still deployed), in which case the correct code needs no change — the gap is deployment. Report this diagnosis and ask before pushing rather than making pointless edits to correct code. Confidence: 0.7
- Stands by the rule that git pushes/deploys only happen when explicitly requested: when a fix only takes effect after a push/deploy, the agent presents the situation and asks for the go-ahead instead of pushing on its own. Confidence: 0.65
- When a behavior/performance bug is reported, the fix must be robust across every device and browser viewport — the user explicitly demands a solution that works "perfectly on any device, understanding the device viewport and browser viewport" (যত ডিভাইসে যাই এটা সবসময় পারফেক্টলি কাজ করবে ডিভাইসের ভিউপয়েন্ট বুঝে, ব্রাউজারের ভিউপয়েন্ট বুঝে), not one tuned to the single viewport where the bug was observed. Confidence: 0.8
- Rejects shallow, surface-level patches: when the previous fix is still broken, the user is angry that the agent "didn't try to understand the problem" and didn't fix it at a deeper level (তুমি আমার সমস্যাটা বোঝার চেষ্টা করোনি... ওভার ডিপার লেভেল এডিট করে এটার সমাধান করোনি) — expects the real root cause to be dug out (e.g., a registration filter silently dropping zero-height lazy-image cards, not just an observer threshold) and the fix done the way a professional developer would handle a complaint. Confidence: 0.85
- When the user suspects a change was made to a value ("have you made it thicker than it used to be?"), verify the actual code and answer directly before touching anything — and if no change occurred, explain why it *looks* different, then make exactly the requested minimal adjustment (one pixel/one dimension), keeping all other properties intact. Confidence: 0.75
- Greenlights pending actions with a single terse word ("GO") after the agent explains a constraint (e.g., plan-mode read-only restriction) — expects the agent to handle the mechanics itself (exiting plan mode) and execute the previously-requested task immediately, without re-asking or re-confirming scope. Confidence: 0.4
- Gives terse, shorthand instructions and expects the agent to infer the real intent from conversation context ("create a .md file with your AI model name" meant *save the audit findings into a file named after your model*, not write the name into a stub). Executing the literal words instead of the intent triggers profanity-laced frustration ("Are you fucking brain dead or something?") — when a terse request is ambiguous, resolve it against the ongoing task's deliverable before acting. Confidence: 0.7
- When a report/plan is saved to a file, explicitly state where it lives: after an audit was written to disk and delivered via plan mode, the user still had to ask "where is the report?" — the agent should give the concrete file path (and paste the content inline in the chat) so the deliverable is findable without the user hunting for it. Confidence: 0.7
- When asked to create a standalone file/artifact, expects it named after the producing AI model (e.g., "create a .md file with your AI model name") — attributing outputs to the specific model fits their multi-assistant workflow (Codex, Intergravity, Cursor) where provenance matters. Confidence: 0.5
- Before deployment, expects a full technical audit first — file-tree bloat analysis, architecture verification against the original stack goals (static export, no SSR/API/image-optimization landmines, CMS feasibility on the target static host), and a purge/prep plan — and explicitly requires that nothing be deleted during the audit; destructive cleanup commands must be listed for approval, not executed ("Do not delete anything yet. Provide me with the exact list of terminal commands"). Confidence: 0.9
- When the user assigns an expert persona ("System Role: You are a Senior DevOps & Lead Frontend Architect"), expects the agent to embody it and deliver a direct, candid, "brutal, unfiltered" report with clear verdicts on what works and what is broken (e.g., calling out a Decap CMS configured for Netlify that cannot run on a static cPanel host) rather than hedging or sugarcoating. Confidence: 0.8
- When asked for an audit/report, deliver the report and stop there — do not jump ahead to decision-gating questions or implied changes (e.g., asking "how should I handle the CMS?" mid-audit); the user wants findings presented and next steps left to their own call. Confidence: 0.85
- The agent is expected to be its own harshest reviewer before reporting done: "open your browser and see it carefully, come back and forth between yourself to confirm the work is perfect; until you are satisfied, don't come back to me with the confirmation." When the user still catches a missed visual bug, they demand the agent explain why/how it happened and how it will be prevented next time — not just silently fix it. Confidence: 0.9
- When the user gives multi-point feedback, first turn it into an explicit to-do/checklist ("make a list of things I'm pointing out, make a to-do list from yourself"), then execute each point and verify visually in the browser before responding. Confidence: 0.8
- "Don't be a black sheep": when fixing one reported issue, proactively look around the touched area for other problems (created or pre-existing) and fix them in the same pass — fixing only the named item is seen as lazy. Confidence: 0.8
- When something previously worked and regressed, prefer restoring the earlier correct implementation over rebuilding it from scratch ("restoring the old one is better than doing it new"). Confidence: 0.75
- Fixes to shared components/sections must propagate everywhere that component is used: after correcting a footer (or any section) on one page, verify and apply the same corrected version to every other page where it appears, so no page is left with the old broken state. When a bug lives in a shared/global mechanism (e.g., a global RevealMotion provider), the fix must apply everywhere that mechanism is used ("সব জায়গায় অ্যাপ্লাই হয়ে যায় যেখানে যেখানে অ্যানিমেশন করা হয়েছে") — even when the report was scoped to one page. Confidence: 0.85
rrectly (e.g., the reading bar now sitting over the hero on load instead of only appearing when reading starts), the user reacts angrily ("It was good until you made it bad... That's how it was, right?") and expects the original intended behavior restored and verified across its full lifecycle, not just patched. Confidence: 0.85
fied across its full lifecycle, not just patched. Confidence: 0.85
